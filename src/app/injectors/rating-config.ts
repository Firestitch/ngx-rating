import { InjectionToken } from '@angular/core';

export const FS_RATING_CONFIG = new InjectionToken<any[]>('fs-rating-config');
export const FS_RATING_DEFAULT_CONFIG = new InjectionToken<any[]>('fs-rating-default-config');
